spec:
  template:
    spec:
      containers:
        - 
          resources:
            requests:
              cpu: "0.2"
#              memory: 256Mi
            limits:
              cpu: "1.0"
#              memory: 256Mi
          env:
          - name: WALLETDB_USERNAME
            value: "marco"
          - name: WALLETDB_PASSWORD
            value: "superman"
          - name: WALLETDB_DATABASE
            value: "tagsystem_wallet"